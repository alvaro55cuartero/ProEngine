#pragma once

#include "ProEngine.h"

class Physics
{

public:
	
	/*
	void OnUpdate(Pro::Timestep delta, Pro::Registry ECS)
	{
		auto view = ECS.GetView<Pro::TransformComponent, Pro::SpriteComponent>();

		for (auto entity : view)
		{
			Pro::TransformComponent transform = ECS.Get<Pro::TransformComponent>(entity);
			Pro::BoxCollider2DComponent collider = ECS.Get<Pro::BoxCollider2DComponent>(entity);
		}
	}
	*/
};
